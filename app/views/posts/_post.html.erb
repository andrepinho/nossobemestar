<%= div_for post do %>
  <h2><%= link_to_unless_current h(post.title), post %></h2>
  <%= simple_format h(post.content) %>
  <%= simple_format h(post.author) %>
  <%= simple_format h(post.published_at) %>  
  <% end %>