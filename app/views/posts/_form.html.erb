<%= form_for(@post) do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "erro impediu", "erros impediram") %> que esse post fosse salvo:</h2>

      <ul>
      <% @post.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= f.hidden_field :region_id, value: current_region.id %>
  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :content %>
    <%= f.cktext_area :content %>
  </div>
  <div class="field">
    <%= f.label :author %>
    <%= f.text_field :author, class: "medium" %>
  </div>
  <div class="field">
    <%= f.label :section_id %>
    <%= f.select :section_id, Section.all.map { |section| [section.name, section.id]} %>
  </div>
  <div class="field">
    <%= f.label :front_title %>
    <%= f.text_field :front_title %>
  </div>
  <div class="field">
    <%= f.label :front_content %>
    <%= f.text_area :front_content %>
  </div>
  <div class="field">
    <%= f.label :ordering %>
    <%= f.select :ordering, [nil,1,2,3,4] %>    
  </div>
  <div class="field">
    <%= f.label :image %>
    <%= f.file_field :image %>    
  </div>
  <div class="field">
    <%= f.label :published_at %>
    <%= f.datetime_select :published_at %>
  </div>
  <div class="actions">
    <%= f.submit class: "button" %>
    <div>ou <a href="javascript:history.back()">cancelar</a></div>
  </div>

<% end %>
