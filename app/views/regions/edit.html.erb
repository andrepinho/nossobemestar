<% content_for :title do %>
  <%= @region.name %>
<% end %>
<h1>Editando região '<%= @region.name %>'</h1>

<%= render 'form' %>

<%= link_to 'Show', @region %> |
<%= link_to 'Back', regions_path %>
